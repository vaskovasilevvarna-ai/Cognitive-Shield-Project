# Fixtures

This directory contains the initial Sprint 0 fixture set.

Current fixtures:
- `clean_message.json`
- `mixed_signal_message.json`
- `ambiguity_heavy_message.json`

Purpose:
- validate basic contract discipline
- support the first bounded integration shell
- preserve a minimal reference set for early testing
